import 'package:flutter/material.dart';

class SchedulePreviewAndEdit extends StatelessWidget {
  const SchedulePreviewAndEdit({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Column(children: [Text('Schedule Preview and Edit')]),
    );
  }
}
